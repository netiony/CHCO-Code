library(BSDA)

tsum.test(
  mean.x = 0.2444,
  s.x = 0.027,
  n.x = NULL,
  mean.y = 0.3106,
  s.y = 0.0287,
  n.y = NULL,
  alternative = "two.sided",
  mu = 0,
  var.equal = FALSE,
  conf.level = 0.95
)